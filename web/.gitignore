# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Generated by `lexicon export-graph` (scripts/build-web.sh runs it before
# every build). Regenerate locally with:
#   cd render && go build -o ./lexicon ./cmd/lexicon
#   ./lexicon export-graph --out ../web/src/data/graph.json --details-dir ../web/public/atoms
src/data/graph.json
public/atoms/

# Generated by `lexicon reading-order` (scripts/build-web.sh runs it before
# every build). Regenerate locally with:
#   cd render && go build -o ./lexicon ./cmd/lexicon
#   ./lexicon reading-order --out ../web/src/data/reading-order.json
src/data/reading-order.json

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
